📚 Digital Library – Web Application
A simple and user-friendly web application for managing books using HTML, CSS, and JavaScript, with offline data storage using localStorage.

🚀 Features
Add, edit, and delete books
Borrow and return books
Search by title or author
Filter by category
Sort by title, newest, or most borrowed
Statistics dashboard (total titles, copies, borrowed)
Persistent storage using localStorage
Export & import book data (JSON)
Clean, responsive UI (mobile-friendly)

🛠️ Tech Stack
HTML5 – Structure
CSS3 – Styling & responsive layout
JavaScript (ES6) – Application logic
localStorage – Client-side data persistence

📁 Project Structure
/digital-library
│── index.html
│── README.md
└── (optional) assets/

📦 How Data is Stored
This project uses localStorage, so all book data is saved inside the browser and stays even after refresh or close.

JavaScript (ES6) – Application logic

localStorage – Client-side data persistence
